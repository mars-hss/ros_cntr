# configuration of ros_cntr

cntr_manager=docker        # Container Manager like docker or podman
ROS_DISTRO="humble"         # Default ros version
bundles="desktop"           # ROS PKG desktop / base / core
ws_name_tag="_ws"           # _ws for workspace
gpu="intel"                 # Change gpu resource from ( nvidia/intel/amd )
port=false		            # true -> Open port for unity connection
multi_distro=false          # multi-containers for same ros version.
multi_ws=false              # multi-containers for same ros and workspace.
